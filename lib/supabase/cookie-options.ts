/**
 * Единые настройки auth-cookie для Supabase SSR клиентов.
 *
 * Цель: сделать сессию persist (переживает закрытие окна браузера),
 * а также синхронизировать параметры между browser / middleware / route clients.
 */
export const SUPABASE_COOKIE_OPTIONS = {
  /**
   * 30 дней — компромисс между UX и безопасностью.
   * Пользователь не должен логиниться заново после закрытия окна.
   */
  maxAge: 60 * 60 * 24 * 30
} as const;
